import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'nextflow.ionic.captest',
  appName: 'NextflowCap',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
