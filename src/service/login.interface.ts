export interface UserData{
    email: string;
    name: string;
    password: string;
    rank: string;
    user_id: string
    }
    export interface LoginData{
        email:string;
        password:string;
      }